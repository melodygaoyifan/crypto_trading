{
  "_p256_note": "BLIND 228 -> 231, total 284 -> 287: the three P256 regimebook-seat writes (agent_signals quant_direction/quant_confidence/signal_edge_bps). They are NOT actually blind — every write sits behind RegimeBookShadow.last_direction(max_age_s=6h), a P156-style staleness bound the P120 scanner cannot see syntactically (it looks for per-write guard patterns). The block is also default-OFF (regimebook_mode). Counters by hand, never --update (P171).",
  "_p265_note": "P265 batch net effect measured 2026-08-14: BLIND 228->229, TIMESTAMPED 13->16, total 284->288, blind-only keys 150->146. Deltas: +3 TIMESTAMPED (staleness-bounded writes: trend-layer stamp, funding-age, hb equity parts), +1 BLIND (the L2 sentiment_direction re-sync — same shape as the L3 FIX-SENT-SYNC write beside it), -2 dead _sentiment_l1/l3_agent_signal writes deleted. Counters by hand, never --update (P171).",
  "_p285_note": "BLIND 229->228, TIMESTAMPED 16->19, total 288->290: the P285 mlp-seat block's agent_signals writes (quant_direction/quant_confidence/signal_edge_bps behind MlpShadow.last_direction's 6h staleness bound — the scanner classes the age-bearing block TIMESTAMPED, the guarded category growing IS the design; one neighboring write reclassified BLIND->TIMESTAMPED within the changed region). Block is default-OFF (mlpshadow_mode). Counters by hand, never --update (P171).",
  "_p287_note": "146->148 (2026-08-16, P287): the two new keys are b6_lead_lag_edge / b6_lead_lag_confidence — RENAMES of the de-collided B6 writes (previously counted under lead_lag_edge/confidence). Not new unguarded signal surface.",
  "blind_only_keys_count": 156,
  "by_category": {
    "BLIND": 242,
    "EXEMPT_TICK_LOCAL": 36,
    "GUARDED": 7,
    "TIMESTAMPED": 28
  },
  "total_writes": 313,
  "_p293_note": "BLIND 228 -> 241, total 290 -> 303, blind-only keys 148 -> 156: the P293 data-coverage repair batch. Attribution is exact. +8 NEW KEYS (the +8 blind-only): deribit_put_call_ratio, deribit_put_call_ratio_volume, deribit_dvol, deribit_mean_iv, exchange_netflow_coins_1d, exchange_netflow_usd_1d, exchange_netflow_pct_1d, exchange_netflow_direction. The remaining +5 writes are re-writes of EXISTING keys inside the two default-OFF enforce branches (exchange_flow, flow_direction, options_short_confirmation, options_confidence, options_put_call_ratio). These are NOT actually unbounded: every one of the 8 new keys is written only when the producing feed reports is_usable() on a cache the feed itself throttles via cache_age_sec()/poll_interval_sec, which is a P156-style staleness bound the P120 scanner cannot see syntactically (it looks for per-write guard patterns). Counters by hand, never --update (P171).",
  "_p293d_note": "TIMESTAMPED 19 -> 22: the three P293d whale-SEAT writes (quant_direction / quant_confidence / signal_edge_bps). They land in the GUARDED-equivalent category because the seat is gated on a freshly-read whale_net_pressure and takes NO seat when the producer is absent. total_writes is unchanged: the seat REWRITES existing keys rather than adding new ones, exactly like the trend/regimebook/mlp seats it mirrors. Counters by hand, never --update (P171).",
  "_p376_note": "total 303 -> 304 (2026-08-22, P376): the one new write site is agent_signals['primary_strategy'] = 'whale' in the whale-seat deciding branch — a new WRITE SITE of the existing primary_strategy key (regimebook already writes it, P313), so the whale seat names itself as the live decider instead of being misattributed to trend_following in the aging/attribution stream. Not new signal surface; a label write, coverage not regression. By hand, never --update (P171).",
  "_p405_note": "TIMESTAMPED 22->25, BLIND 241->240 (2026-08-24, P405): the ETF-flow seat's agent_signals writes (quant_direction/quant_confidence/signal_edge_bps behind EtfFlowShadow.seat_direction's freshness+12h-age bound -> TIMESTAMPED, like the mlp/whale seats; quant_data_quality/primary_strategy re-write existing keys). Seat default-OFF in code, armed only in the live profile. By hand, never --update (P171).",
  "_p407_note": "+4 total (TIMESTAMPED +3, BLIND +1): the [P407] skew direction seat's 5 agent_signals writes (quant_direction/confidence/edge/data_quality/primary_strategy), same footprint as the P405 ETF seat. Isolated-worktree attributed.",
  "_p420_note": "BLIND 241 -> 242, total 312 -> 313 (2026-08-27, P420): the ONE new write site is market_data['whale_confidence'] = the P352 evidence-gated confidence at the whale bridge in main.py (a KNOWN whale_count below the evidence floor zeroes the confidence in BOTH dicts, P170 two-dict rule). Same tick as the agent_signals write beside it; syntactically blind to the P120 scanner, not new unguarded signal surface. blind_only_keys_count unchanged (existing key). By hand, never --update (P171)."
}