{
  "_p216_note": "336 was 334: +2 for CCNewsFeed._restore_state/_persist_state (P216). Both are deliberate fail-soft guards mirroring the accepted CryptoPanic precedent from P154 \u2014 a corrupt state file must not break startup, and telemetry must not break the tick. Both LOG on failure (warning / debug) and carry noqa: silent-swallow; this counter is produced by silent_failure_audit, which counts try/except blocks generally and is not noqa-aware. Updated this ONE counter by hand: --update re-seeds all seven baselines including mypy (P171).",
  "_p221_note": "337 was 336: +1 for BlockchairOnChainFeed._f (P221), a numeric coercion helper. Blockchair returns wei as a STRING and absent fields as null, so both are expected data shapes rather than errors; logging each would drown the feed. It carries noqa: silent-swallow and the swallow scanner accepts it \u2014 this counter comes from silent_failure_audit, which counts try/except generally and is NOT noqa-aware. Updated this ONE counter by hand; --update re-seeds all seven including mypy (P171).",
  "_p226_note": "Counts ROSE because these scanners started SEEING main.py, NOT because defects were added. Every read used encoding=\"utf-8\" while main.py carries a UTF-8 BOM, so ast.parse raised SyntaxError on U+FEFF and the largest file in the repo was silently skipped for as long as these audits have existed. Attributed BEFORE re-baselining (P175): all 308 new silent_failure entries are main.py symbols (ExecutionContext.build_from_runner, MahalanobisOODDetector.load, *Config.from_dict ...), and both counts are now identical with and without the BOM. Same defect as P171 in lint_orphan_signal_reads.py, never applied here.",
  "_p236_note": "651 -> 652: the [MA-FILTER] fail-soft guard in the run_live sleeve loop (main.py, P236). Deliberate: the filter is an overlay on the sole order path \u2014 a fault in it must never stop management; it logs WARNING naming the consequence (sleeve managed UNFILTERED). Counter bumped by hand, never via --update (P171: --update re-seeds the mypy baseline too).",
  "_p240_note": "652 -> 653: +1 for the FastRiskTick position-hint guard in main.py (P240). It is a SEVERITY hint only and falls back to None (= keep alerting at CRITICAL) on any error, so swallowing is the correct behaviour \u2014 raising there would let a logging nicety break the 30s risk loop. Carries noqa; this counter comes from silent_failure_audit, which is not noqa-aware. Single counter updated by hand \u2014 --update re-seeds all seven including mypy (P171).",
  "_p252_note": "653 -> 661, attributed via P175 scratch-tree diff vs the stamping commit (6d359e1): +7 from the P242-P250 regime-book shadow + funding-history persistence fail-soft sites (all log via _warn_once or are value coercion), +1 from P251's target_for_signal call inside the existing cooldown try. The P24x commits shipped without the gate being read \u2014 the P233 trap; found because CI went red and was DIFFED, not glanced.",
  "_p253_note": "661 -> 663: +2 for the P253 fail-soft guards in run_live (main.py): the loop-level _save_paper_positions guard (logs ERROR naming the consequence: governor/fuse state STALE on disk) and the daily-PnL-anchor/SOTA-state restore guards in _load_paper_positions (log WARNING; a malformed payload must not block boot, and both restores are one-directional so a failed restore only delays a control, never weakens it). Single counter updated by hand - never --update, which re-seeds all seven baselines including mypy (P171). The local mypy +7 on this machine is the P227 environment fingerprint (was +16; P253 deleted a dead import edge worth -9 union-attr errors) and is deliberately NOT re-baselined.",
  "_p253c_note": "663 -> 664: the P253c sweep converted the tranche-level bare \"except Exception: pass\" (main.py _sync_tranche_scheduler_with_paper_positions) into a LOGGED fail-soft guard - the only try/except-shaped line in the sweep diff (attributed via git diff, not assumed). The audit counts the logged handler where it did not count the bare pass; the change is strictly an observability improvement (the swallow now names the TrancheLevel enum drift it exists to catch). Single counter updated by hand - never --update (P171).",
  "_p253d_note": "664 -> 665: +1 for the P253d warn-only config-schema validation guard at boot (main.py main()). Deliberate fail-soft: converting a schema finding into a boot refusal would be a NEW failure mode on the live startup sequence (the P85 restart-loop class), so the guard logs and proceeds. The P253d cc_onchain backoff persistence helpers carry their own logged noqa swallows outside this counter scope. Single counter updated by hand - never --update (P171).",
  "_p256_note": "665 -> 666: +1 for the adjusted-ledger fail-soft guard in defense/regime_book_shadow.record_tick (P256) \u2014 the adjusted forward ledger is an overlay; its failure logs a WARNING and must never lose the raw record. Single counter by hand, never --update (P171).",
  "_p263_note": "666 -> 668: the P263 startup sleeve build+snapshot guard and the heartbeat reconciled-book text guard \u2014 both log (warning / the driver's own logs carry the truth) and both must be fail-soft: neither startup reconcile nor heartbeat composition may stop the engine (P227 rule). Hand-bumped, never --update.",
  "_p264_note": "+1 for the P264 ts-parser block in analytics/shadow_ic/compute_shadow_ic.py: the except tuple widened from ValueError to (ValueError, OSError, OverflowError) to cover epoch-float edge cases (fromtimestamp raises OSError/OverflowError on absurd values), which moved the block into the audit counter. Same behavior as before: the skip is COUNTED in `skipped` and batch-WARNED at end of load, noqa-annotated. Attributed by single-file bisect (revert -> clean, restore -> +1). Hand-bumped, never --update (P171).",
  "_p272_note": "669 -> 670: the passive-holdings advisory guard in run_live's heartbeat block (main.py, P272). Deliberate fail-soft: the advisory is heartbeat TEXT about assets the operator holds outside the system \u2014 a fault in it must never break the heartbeat, and the harness's own logs carry any real failure. Carries noqa: silent-swallow; this counter comes from silent_failure_audit which is not noqa-aware. Bumped by hand, never via --update (P171).",
  "_p273_note": "670 -> 671: the per-symbol fetch_ticker guard inside _unmanaged_holdings_text (main.py, P273). An unlisted pair or dust symbol is an EXPECTED data shape, not an error \u2014 it renders as '(unpriced)' in the display-only heartbeat line, never as $0 (absence is not zero, P2). noqa'd; counter is not noqa-aware; bumped by hand, never --update (P171).",
  "_p277_note": "671 -> 672: the _enh_oi_stash guard in the per-asset tick (main.py, P277). A stash miss means one flat oidiv ledger row, never a tick fault; noqa'd; counter not noqa-aware; bumped by hand, never --update (P171).",
  "_p282_note": "672 -> 676: four fail-soft guards from P281/P282 \u2014 calbasis state restore + persist (enhancement_shadows, both log via warning/transition), the CDE funding-history accrual append (main.py, noqa'd: an archive write must never touch the tick; gaps are visible in the file itself), and ds_model_pipeline's window-ledger record guard (prints its failure). Bumped by hand with attribution, never --update (P171).",
  "_p284_note": "676 -> 678: the mlp_shadow per-asset tick guard (summary line shows the miss; a shadow fault must never reach the loop) and main.py's fv2-fetch guard (noqa'd: fv2 absence records as a NAMED coverage gap inside the harness, never a fabricated zero \u2014 the P248 rule). Hand-bumped, never --update (P171).",
  "_p285_note": "678 -> 679: the P285 mlp-seat block's fail-soft guard in main.py (a seat fault must never break the tick; it logs WARNING naming the consequence \u2014 incumbent signal stands). Default-off block. Hand-bumped, never --update (P171).",
  "_p287_note": "679->680 (2026-08-16, P287 fix campaign): one scanner-visible guard among the main.py hardening (fuse-feed freshness / governor-restore wraps \u2014 all log, none silent). The campaign's other new try/excepts (core [P287-FUSE] x3, exchange sleeve x3, defense mlp_shadow persistence x2) fall outside this counter's shape.",
  "_p289_note": "680->682 (2026-08-16, P289): the [VENUE-SPREAD] fail-soft guard in main.py's VENUE-FEE block (logs, Kraken table stands on failure) and the trend_rule_shadow harness guard (logs; Iron Law 7 fail-soft). Both log; neither silent.",
  "_p293_note": "691 was 682: +9 for the P293 data-coverage repair batch. Attribution: +3 main.py (the Deribit options bridge, the exchange-netflow bridge and the F&G historical-z lookup, all loop-level fail-soft guards on the decision path - a data feed must never be able to stop a tick, Iron Law 7); +2 deribit_feed (fetch, and the per-currency DVOL leg whose failure must not lose the PCR); +2 exchange_netflow_feed (fetch, per-symbol isolation); +1 fear_greed_history (refresh_if_stale); +1 global_context_informer (_fetch_ticker_fred). Every one LOGS on failure and several carry noqa: silent-swallow; this counter comes from silent_failure_audit, which counts try/except blocks generally and is NOT noqa-aware (P216). Updated this ONE counter by hand: --update re-seeds all seven baselines including mypy (P171).",
  "_p293b_note": "694 was 691: +3 for the P293b quota/restart batch. Attribution: +1 cryptopanic_feed (reading the 429 BODY to tell a MONTHLY QUOTA from a transient rate limit - a body read must not be able to break the error path); +1 cryptocompare_onchain (per-row coercion while restoring the persisted payload, so one corrupt row cannot lose the rest); +1 global_context_informer (_fetch_aggregate_flow_coinglass, whose failure must degrade to 'ETF flow absent', never to a fabricated 0.0). All three LOG or return an explicit absence. Counter by hand, never --update (P171).",
  "_p293d_note": "695 was 694: +1 for the P293d whale-filter guard in the sleeve driver (the overlay must never stop the order path, Iron Law 7 \u2014 it logs a WARNING naming that the sleeve was managed UNFILTERED, mirroring the P236 MA-filter guard beside it). Counter by hand, never --update (P171).",
  "_p303_note": "695 -> 697: the two guards in RegimeBookShadow._restore_last_records - one per bad ledger row (skip that file, keep the rest) and one around the whole restore (a failed restore is exactly today behaviour: no seat on the first tick). Both are annotated at source and the outer one LOGS. Hand-bumped with attribution, never --update (P171).",
  "_p304_note": "697 -> 699: the guards in HMATSProductionRunner._cascade_liq_delta. An unreadable state file is a cold start (returns None, which the caller treats as 'no previous reading' and falls back to the old behaviour, never as zero liquidations), and a failed write is observation-only. Both annotated at source. Hand-bumped with attribution, never --update (P171).",
  "_p306_note": "699 -> 707: the guards added by the P306 measured-input batch, all logged or annotated at source. data_mgmt/feeds/kraken_hourly.py: the urlopen and the payload parser (a malformed body yields no rows rather than raising into a tick). data_mgmt/feeds/dvol_history.py: fetch, parse, persist and restore, plus the value-coercion refusals in zscore() - every one of them returns None or an empty history, which the caller treats as \"no key is written\", never as a fabricated z. main.py::_publish_real_1h_change: the feed import/read (falls back to the pre-P306 rescale) and its own logging. risk/cascade_exhaustion_governor.py: all_governor_states / restore_governor_states / the reset loop - persistence and teardown only, and a failed restore is a cold start at phase NONE. Hand-bumped with attribution, never --update (P171).",
  "_p313_note": "P313 adds THREE logged guards to this counter: (1) the P313-A decoupling SPLIT the single try/except that wrapped the leverage write and the funding write into two independent guards \u2014 the funding wire had to leave the `regime_leverage_enabled` block, and each half must now fail independently rather than one exception skipping both; (2) ProductionConfig._apply_late_config_overrides converts a malformed config VALUE into a logged warning that KEEPS the dataclass default, rather than letting a bad line raise through from_file and take the boot down (P85) or silently install a wrong-typed value; (3) HEALTH_T1's direction coercion, which treats an unparseable direction as UNKNOWN rather than flat so the check keeps warning instead of going silent (P2). All three LOG or are noqa-annotated at source. The ABSOLUTE count (709) is the whole tree's and is NOT this entry's arithmetic alone: parallel sessions (P311/P311b/P312/P314) added and deleted guards in the same working tree during this change, so the delta was re-derived against the CURRENT HEAD rather than carried from an earlier reading. Bumped by hand \u2014 --update re-seeds all seven baselines including mypy (P171) \u2014 and attributed before bumping (P175): an earlier draft nearly accepted an unexplained +1 that turned out to be another session's concurrent edit to this very file, not a real finding.",
  "dictget_count": 42,
  "flags_count": 0,
  "tryexcept_count": 710,
  "_p319_note": "709 -> 710 (+1), hand-bumped with attribution, never --update (P171). The single new guard is the [P319] one-shot combined-currency probe in data_mgmt/feeds/cryptopanic_feed.py: a measurement must never be able to break the feed it measures, so its request is wrapped and the failure is logged with type(e).__name__. It carries a `# noqa: silent-swallow`, but THIS counter comes from silent_failure_audit.py, which is not noqa-aware (P223) \u2014 hence the bump rather than a suppression. Measured 8 -> 9 `except` in that file; a concurrent +1 seen locally came from another session's UNCOMMITTED files and is deliberately not absorbed here."
}
