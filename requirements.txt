# ================================================================================
# HMATS v5.4.0 - Production Runtime Dependencies
# ================================================================================
# Purpose: Minimal dependencies for cloud production runtime (verify/paper/live)
# Excludes: Training libraries (PyTorch, stable-baselines3, GPU-specific)
# 
# Install: pip install -r requirements.txt
# ================================================================================

# ------------------------------------------------------------------------------
# Core Scientific Computing (required for all trading logic)
# ------------------------------------------------------------------------------
numpy>=1.24.0,<2.0.0          # Arrays, math operations (used everywhere)
pandas>=2.0.0,<3.0.0          # DataFrames for OHLCV, positions (used in execution, risk)
scipy>=1.10.0,<2.0.0          # Statistics, signal processing (used in regime detection)

# ------------------------------------------------------------------------------
# Machine Learning (runtime only - sklearn for regime classification)
# ------------------------------------------------------------------------------
scikit-learn>=1.3.0,<2.0.0    # Regime classifier GMM (market/regime_navigator.py)
filterpy>=1.4.0,<2.0.0        # Kalman filters (agents/quant_agent.py)

# ------------------------------------------------------------------------------
# Async & Networking (required for Kraken WebSocket/REST)
# ------------------------------------------------------------------------------
aiohttp>=3.8.0,<4.0.0         # Async HTTP client (kraken_defensive_link.py)
websockets>=11.0,<13.0        # WebSocket connections (exchange/kraken/)
asyncio-throttle>=1.0.0       # Rate limiting for async calls

# ------------------------------------------------------------------------------
# HTTP Clients (required for REST API)
# ------------------------------------------------------------------------------
requests>=2.28.0,<3.0.0       # Sync HTTP (defense/watchdog.py, data_provider.py)
urllib3>=1.26.0,<3.0.0        # HTTP foundation

# ------------------------------------------------------------------------------
# Exchange API
# ------------------------------------------------------------------------------
krakenex>=2.1.0,<3.0.0        # Kraken REST API client

# ------------------------------------------------------------------------------
# Data Validation & Configuration
# ------------------------------------------------------------------------------
pydantic>=2.0.0,<3.0.0        # Config/data validation
python-dotenv>=1.0.0,<2.0.0   # Environment variable loading (.env)

# ------------------------------------------------------------------------------
# Time & Date Handling
# ------------------------------------------------------------------------------
python-dateutil>=2.8.0,<3.0.0 # Date parsing, timezone handling
pytz>=2023.0                   # Timezone definitions

# ------------------------------------------------------------------------------
# Logging & Monitoring
# ------------------------------------------------------------------------------
structlog>=23.0.0,<25.0.0     # Structured logging

# ------------------------------------------------------------------------------
# Optional: Dashboard (uncomment if running Streamlit dashboard in production)
# ------------------------------------------------------------------------------
# streamlit>=1.28.0,<2.0.0    # Dashboard UI (dashboard/app.py)
